name: Deploy (placeholder)

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy pipeline will be implemented once hosting targets are chosen (web + api + migrations)."
